# This rule is triggered when the "autonomicFileDetected" event is caught.
# It runs on the Roboconf Deployment Manager.
rule "autonomicFileDetected"
when
	autonomicFileDetected
then
	logFileEvent
end
